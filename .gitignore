CrossTrainer_Proposta.html
Vendas realizadas CP fev2026.xlsx
DOCUMENTACAO.md
commission.txt
index.txt
indexOLD.html
.claude/settings.local.json
.claude/worktrees/

# Estado local de ferramentas
.superpowers/

# Planilhas de vendas (dados, não código)
vendas*.xlsx
vendas*.xls

# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log
ui-debug.log

# Service Accounts (NUNCA commitar — contêm credenciais privadas)
scripts/serviceAccount-*.json
functions/serviceAccount-*.json

# Node
node_modules/
npm-debug.log*

# Build artifacts
.cache/
dist/

# Sprint 9 — Preview HTML temporário de recibos (descartável)
tmp-preview-recibos/

# Estado runtime das fixtures (não é código — rastreia docs criados pra cleanup)
scripts/.fixture-*-state.json
backups/

# Planilhas e anotações de trabalho — NUNCA no repositório (ele é PÚBLICO).
# Entraram por engano em 3c4cccf (git add -A); removidas em seguida.
*.xlsx
ajustes a fazer*.txt
docs/rodrigo-roteiro-completo.txt
apresentacao-modulo-professores.html
.claude/launch.json
.claude/settings.json

# Exports da Pacto — dado de cliente (nome, contrato, valor). Repositorio publico.
faturamento*.xls
faturamento*.xlsx
relatorios pacto/
carga nova comissoes/
PACTO-*.xlsx
